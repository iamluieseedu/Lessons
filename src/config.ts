// Global default settings built directly into the codebase.
// Students on any device or profile will load these settings by default.
export const CONFIG = {
  // Paste your Google Sheets Webhook URL here:
  webhookUrl: '',
  
  // Paste your Google Cloud Client ID here:
  googleClientId: '',
  
  // Paste your allowed domain here (e.g. '@school.edu.ph'):
  allowedDomain: '',
};
